// Language Translation functions - Sinhala and English

const translateToEnglish = (text) => {
  // Code to translate Sinhala text to English
  // This is a placeholder. You can integrate Google Translate or other API
  console.log(`Translating Sinhala to English: ${text}`);
  return `Translated text: ${text}`;
};

const translateToSinhala = (text) => {
  // Code to translate English text to Sinhala
  // This is a placeholder. You can integrate Google Translate or other API
  console.log(`Translating English to Sinhala: ${text}`);
  return `පරිවර්තනය කළ වාක්‍ය: ${text}`;
};

module.exports = { translateToEnglish, translateToSinhala };
